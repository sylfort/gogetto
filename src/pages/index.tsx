import type { NextPage } from "next";
import Head from "next/head";
import { trpc } from "../utils/trpc";

const Home = () => {
  return (
    <main>
      <h1>Gogetto</h1>
    </main>
  );
};

export default Home;